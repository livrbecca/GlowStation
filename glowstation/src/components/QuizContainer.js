import Quiz from "react-quiz-component";
import { quiz } from "./Quiznpm";
import React from "react";

const QuizContainer = () => {
  <div>
    <Quiz quiz={quiz} shuffle={false} />
  </div>;
};

export default QuizContainer;
